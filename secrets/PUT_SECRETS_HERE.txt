Use this folder for your secrets. Secrets are handled in docker-compose.yml, which says what filenames to use.
Each secret is its own file, the contents of which are nothing but a single line containing the secret.
